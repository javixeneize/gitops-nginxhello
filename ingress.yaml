apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  labels:
    app: nginxhello2
  name: single-default-backend-ingress
  annotations:
    fluxcd.io/ignore: "false"
spec:
  backend:
    serviceName: nginxhello2
    servicePort: 80
